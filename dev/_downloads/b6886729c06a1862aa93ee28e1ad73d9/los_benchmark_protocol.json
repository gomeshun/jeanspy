{
  "schema_version": 1,
  "protocol_id": "matched-los-accuracy-time-v1",
  "status": "frozen_before_execution",
  "baseline_commit": "1a0ad4028d26af1df389ebdfdf992285ec50f8bb",
  "author_direction": "2026-09-14 JST: omit MCMC comparisons; benchmark matched forward functions, prioritizing line-of-sight velocity dispersion accuracy and evaluation time.",
  "scope": "A fresh, bounded forward-only experiment. The Plummer cases and JAM settings are inherited from the already observed jam9-plummer-isotropic-v1 validation; this is not an unseen-model accuracy test. Freeze this protocol and runner in git before any new evaluation or timing.",
  "physical": {
    "a_pc": 300.0,
    "mass_Msun": 10000000.0,
    "tracer_q": [1.0, 0.7],
    "inclination_rad": 1.1,
    "halo_Q": 1.0,
    "alpha": 2.0,
    "beta": 5.0,
    "gamma": 0.0,
    "beta_z": 0.0,
    "cutoff": "infinite"
  },
  "observable": "Surface-density-weighted LOS second moment in (km/s)^2, with no PSF, pixel averaging or measurement noise. Its square root is sigma_los for the imposed zero-mean-velocity convention, otherwise v_rms; no streaming decomposition is inferred.",
  "coordinates": {
    "counts": [16, 64, 256],
    "radius_min_pc": 10.0,
    "radius_max_pc": 1500.0,
    "azimuth_step": 0.6180339887498949,
    "definition": "r=geomspace(rmin,rmax,N); phi=2*pi*((arange(N)*azimuth_step)%1); x=r*cos(phi), y=q_projected*r*sin(phi). N is the number of sky positions, not an observed stellar sample."
  },
  "axisymmetric_orders": [16, 32, 64],
  "spherical_classical_orders": [128, 256, 512],
  "spherical_jax_orders": [32, 64, 128],
  "spherical_settings": {"n_kernel": 128, "u_max": 100000.0, "kernel_outer_transform": "sqrtlog", "dm_mass_method": "numeric", "dm_mass_n_steps": 128},
  "jam_version": "9.0.2",
  "mgefit_version": "6.2.6",
  "jam_projection": [
    {"ngauss": 24, "nang": 9, "nlos": 30},
    {"ngauss": 32, "nang": 15, "nlos": 60},
    {"ngauss": 48, "nang": 21, "nlos": 90}
  ],
  "mge_fit": {"rmin_pc": 0.01, "rmax_pc": 100000.0, "samples": 600, "inner_slope": 0, "outer_slope": 5, "assessment_range_pc": [1.0, 10000.0]},
  "jam_settings": "Callable analytic potential containing JeansPy G, tracer MGE reused for fixed tracer shape, beta=0, ml=1, interp=False, spectral_derivs=True, moment=zz, sigmapsf=0, pixsize=0, distance=0.648/pi Mpc (one arcsec per pc). Recompute the projected solver on every call; do not replace its internal radial-grid heuristic.",
  "reference": {
    "spherical": "Exact Plummer result sigma_los^2=3*pi*G*M/(64*sqrt(a^2+x^2+y^2)).",
    "oblate": "Independent analytic hypergeometric vertical pressure and its analytic radial derivative, followed by SciPy adaptive infinite LOS quadrature. Derivation is in the runner and public report; no JeansPy force or quadrature routine is used.",
    "epsabs": [1e-10, 2e-12],
    "epsrel": [1e-10, 2e-12],
    "quad_limit": 300,
    "intrinsic_check_points_in_a": [[0.1, 0.2], [0.3, 0.0], [1.0, 0.7], [3.0, 1.5]],
    "max_relative_refinement": 1e-8,
    "max_relative_intrinsic_integral_check": 1e-8,
    "max_relative_spherical_analytic_check": 1e-8
  },
  "accuracy": {
    "primary_second_moment_relative_limit": 0.005,
    "display_relative_levels": [0.01, 0.005, 0.001, 0.0001],
    "report": "Store signed pointwise errors, maximum relative second-moment and square-root errors, RMS relative errors, finite-positive gates, and accuracy for every timed mass normalization. Report the complete accuracy-time curve, including failures; compare ratios only within a named accuracy threshold. No post-result order/threshold selection."
  },
  "timing": {
    "warm_repeats": 10,
    "mass_factors": "1 + 0.01*sin(arange(10)*0.71); the first call uses factor 1. All engines recompute predictions for the same fixed mass-normalization sequence with fixed coordinates and tracer.",
    "stages": ["imports and device initialization", "coordinates and model construction", "JAM tracer MGE fitting (once per shape/resolution, reusable across N and potential parameters)", "synchronized JAX host-to-device input transfer", "first prediction (JAX tracing, compilation and execution included)", "ten warm predictions with changed mass normalization", "JAX device-to-host result transfer"],
    "cache_policy": "Clear JAX caches before each shape/N/solver/order row; keep caches for its ten calls. Coordinates and all physical parameters are dynamic JIT arguments. NumPy and JAM recompute their ordinary public forward call each time. Synchronized device-resident timing excludes separately measured result transfer.",
    "report": "Retain all ten durations, median, p10 and p90. Percentiles describe within-process timing variation, not statistical confidence across hosts. Do not mix fitting, first-call and warm costs."
  },
  "conditions": ["numpy-cpu-float64", "jax-cpu-float64", "jax-gpu-float64", "jam-cpu-float64"],
  "resources": {"cpu_affinity": [0, 1, 2, 3, 4, 5, 6, 7], "blas_openmp_threads": 1, "host_rss_gib": 16, "gpu_memory_gib": 16, "max_seconds": {"reference": 600, "numpy": 1800, "jax-cpu": 1200, "jax-gpu": 1200, "jam": 1800}},
  "accounting": "Charge every execution, including failed attempts, to the existing single 86400-second campaign ledger. Check the remaining balance before launch. No new ledger or automatic budget extension.",
  "failure_rule": "Preserve each attempted row, warning, exception, failed accuracy gate and resource stop. Save after each stage and repetition. A failed independent reference blocks interpretation and timing until an explicitly labeled, frozen correction; do not silently overwrite or retune.",
  "limitations": "JAM spherical alignment and JeansPy cylindrical alignment coincide in this beta=beta_z=0 subset. The potential is spherical and smooth; this does not test flattened halos, cusps, anisotropic closures or posterior accuracy. The specialized spherical JeansPy solver is measured only for q=1. Existing NFW/flattened-Zhao forward and gradient studies remain complementary evidence. No new MCMC or paper experiment is authorized by this protocol."
}
